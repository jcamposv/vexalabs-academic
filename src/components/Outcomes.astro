---
import SectionHeading from './SectionHeading.astro';

const outcomes = [
  '4 aplicaciones SaaS construidas',
  'Autenticación y roles implementados',
  'Base de datos PostgreSQL en producción',
  'Emails transaccionales funcionando',
  'Deploy profesional en Vercel',
  'Sistema de pagos con LemonSqueezy',
  'Workflow profesional con AI',
  'Portfolio real — no proyectos de tutorial',
];
---

<section id="resultados" class="bg-surface-alt py-12 sm:py-16" aria-labelledby="outcomes-heading">
  <div class="mx-auto max-w-6xl px-4 sm:px-6">
    <SectionHeading
      label="Resultado final"
      title="Lo que vas a tener al terminar"
    />

    <div class="mx-auto max-w-2xl">
      <div class="grid gap-3 sm:grid-cols-2">
        {outcomes.map((o) => (
          <div class="flex items-center gap-3 rounded-xl border border-border bg-surface-muted p-4 transition-shadow duration-200 hover:shadow-md hover:shadow-brand-500/5">
            <svg class="h-5 w-5 shrink-0 text-brand-500" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2.5" aria-hidden="true">
              <path stroke-linecap="round" stroke-linejoin="round" d="M4.5 12.75l6 6 9-13.5" />
            </svg>
            <span class="text-sm font-medium text-text">{o}</span>
          </div>
        ))}
      </div>

      <div class="mt-10 text-center">
        <p class="text-lg text-text-muted">
          En 4 semanas pasás de <span class="text-text font-semibold">"quiero hacer un SaaS"</span>
        </p>
        <p class="text-lg text-brand-500 font-bold">
          a "mi SaaS ya está en producción".
        </p>
      </div>
    </div>
  </div>
</section>

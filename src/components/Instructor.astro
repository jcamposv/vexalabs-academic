---
import SectionHeading from './SectionHeading.astro';
import Badge from './Badge.astro';

const experience = [
  { company: 'EarnUp', role: 'Full Stack Developer', detail: 'Microservicios con FastAPI, Next.js, integración con Plaid' },
  { company: 'SoftwareONE', role: 'Tech Lead / Full Stack Engineer', detail: 'Sistemas de billing global para 200+ países, arquitectura AWS' },
  { company: 'Svitla Systems', role: 'Senior React Developer', detail: 'Apps enterprise de alta performance, code reviews, mentoring' },
  { company: 'Janeiro Digital', role: 'Senior Frontend Developer', detail: 'Arquitecturas frontend escalables' },
  { company: 'Prodigious (DigitasLBi)', role: 'Senior Engineer', detail: '4+ años construyendo plataformas web a escala' },
];

const products = [
  { name: 'GymGo.io', desc: 'Plataforma de gestión para gimnasios' },
  { name: 'Edcora.ai', desc: 'Asistente AI para profesores' },
  { name: 'Mantenix.com', desc: 'SaaS de mantenimiento y órdenes de trabajo' },
];

const highlights = [
  'Sistemas de billing global manejando facturación de AWS y Azure en 200+ países',
  'Resolución de incidentes críticos en arquitecturas de microservicios',
  'Plataformas AI con OpenAI, CrewAI y sistemas basados en agentes',
  'AWS Certified Developer — Associate',
];
---

<section id="instructor" class="bg-surface-alt py-12 sm:py-16" aria-labelledby="instructor-heading">
  <div class="mx-auto max-w-6xl px-4 sm:px-6">
    <SectionHeading label="Instructor" title="Tu instructor" />

    <div class="mx-auto max-w-4xl">
      <!-- Header -->
      <div class="flex flex-col items-center gap-6 text-center sm:flex-row sm:text-left">
        <img
          src="/jairo.png"
          alt="Jairo Campos"
          class="h-28 w-28 shrink-0 rounded-full object-cover border-2 border-brand-500/30"
          loading="lazy"
        />
        <div>
          <h3 id="instructor-heading" class="text-2xl font-bold text-text">Jairo Campos</h3>
          <p class="mt-1 text-sm font-medium text-brand-500">Senior Full Stack Engineer · Founder & Tech Lead</p>
          <p class="mt-1 text-sm text-text-muted">Costa Rica · 14+ años en software · React, Next.js, Python, AI</p>
        </div>
      </div>

      <!-- Bio -->
      <div class="mt-8 rounded-2xl border border-border bg-surface-muted p-6">
        <p class="text-text-muted leading-relaxed">
          No soy un "influencer de AI". Soy un ingeniero que construye productos reales todos los días. He trabajado en startups, empresas globales y como Tech Lead diseñando arquitecturas que escalan. Hoy construyo mis propios SaaS aplicando todo lo que enseño en este curso.
        </p>
      </div>

      <!-- Two columns: experience + products -->
      <div class="mt-8 grid gap-6 sm:grid-cols-2">
        <!-- Career highlights -->
        <div>
          <p class="mb-4 text-sm font-bold uppercase tracking-wider text-text-muted">Trayectoria</p>
          <div class="space-y-3">
            {experience.map((e) => (
              <div class="rounded-xl border border-border bg-surface-alt p-3">
                <div class="flex items-center justify-between gap-2">
                  <span class="text-sm font-semibold text-text">{e.company}</span>
                  <span class="shrink-0 text-xs text-text-muted">{e.role}</span>
                </div>
                <p class="mt-1 text-xs text-text-muted">{e.detail}</p>
              </div>
            ))}
          </div>
        </div>

        <!-- Products + highlights -->
        <div>
          <p class="mb-4 text-sm font-bold uppercase tracking-wider text-text-muted">Productos propios</p>
          <div class="space-y-3">
            {products.map((p) => (
              <div class="flex items-center gap-3 rounded-xl border border-border bg-surface-alt p-3">
                <svg class="h-4 w-4 shrink-0 text-brand-500" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2.5" aria-hidden="true">
                  <path stroke-linecap="round" stroke-linejoin="round" d="M4.5 12.75l6 6 9-13.5" />
                </svg>
                <div>
                  <span class="text-sm font-semibold text-text">{p.name}</span>
                  <span class="text-xs text-text-muted"> — {p.desc}</span>
                </div>
              </div>
            ))}
          </div>

          <p class="mb-3 mt-6 text-sm font-bold uppercase tracking-wider text-text-muted">Logros destacados</p>
          <ul class="space-y-2">
            {highlights.map((h) => (
              <li class="flex items-start gap-2 text-xs text-text-muted">
                <svg class="mt-0.5 h-3.5 w-3.5 shrink-0 text-brand-500" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2.5" aria-hidden="true">
                  <path stroke-linecap="round" stroke-linejoin="round" d="M4.5 12.75l6 6 9-13.5" />
                </svg>
                <span>{h}</span>
              </li>
            ))}
          </ul>
        </div>
      </div>

      <!-- Quote -->
      <div class="mt-8 rounded-xl border border-brand-500/20 bg-brand-50 p-5 text-center">
        <p class="text-sm text-text italic">
          "Todo lo que enseño, lo uso. Todo lo que construimos en el curso, lo hago en mi trabajo real."
        </p>
      </div>
    </div>
  </div>
</section>
